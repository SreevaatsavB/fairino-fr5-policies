# NOTE: install into a fresh venv — lerobot 0.5.1 caps torch/vision/numpy below
# the very latest releases, so it conflicts with a bleeding-edge system stack.
#   python3.12 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt

# core
numpy>=2.0,<2.3
pandas>=2.2
pyarrow>=14.0
pyyaml>=6.0

# viz / eda
matplotlib>=3.7
jupyterlab>=4.0

# deep learning (versions pinned to what lerobot 0.5.1 supports)
torch>=2.7,<2.11
torchvision>=0.22,<0.26

# lerobot (pulls in transformers, huggingface-hub, datasets, opencv-headless,
# torchcodec, gymnasium, etc.). The API used by training/model.py targets 0.5.x.
# NOTE: lerobot's pi extra pins transformers==5.3.0 (the version its pi0/pi05
# checkpoints were written against); common/vla_pretrained.py remaps keys so the
# pi policies also load correctly on newer transformers.
lerobot==0.5.1

# LoRA adapters for the pi0/pi05 VLM (see common/vla_pretrained.py)
peft>=0.17

# scipy — required by pi0_fast's FAST action tokenizer (lerobot/fast-action-tokenizer);
# PI0FastPolicy() fails to construct without it.
scipy>=1.11
